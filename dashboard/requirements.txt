# The dashboard (server.py + static/) needs NOTHING beyond the Python standard
# library -- just run: python server.py

# Only needed when talking to real hardware over USB / Bluetooth-Classic
# (SerialSource); the simulator does not need it.
pyserial>=3.5
